import "./Loading.css";

const Loading = () => {
    return (
        <div className="loading">
            <h1>Uploading...</h1>
            <progress></progress>
        </div>
    );
};

export default Loading;
